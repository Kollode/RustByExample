fn main() {
    println!("Hello, world!");
    println!("I'm awesome");

    // println!("Hello, world!") without ; will work
    // but only if there is no second println!.
    // Probably because the println is a expression and can
    // be returned
}
